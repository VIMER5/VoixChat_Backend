syntax = "proto3";
package discovery;
service discoveryService {
  rpc registerService (serviceInfo) returns (infoResponse);
  rpc heartbeat (serviceInfo) returns (infoResponse);
  rpc getServicesbyName (servicesNameRequest) returns (ListServiceInstancesResponse);
}

message serviceInfo {
  string nameService = 1;
  string ipService = 2;
  int32 portService = 3;
}

message infoResponse {
  bool success = 1;
  string message = 2;
}

message ServiceInstance {
  string id = 1;
  string nameService = 2;
  string ipService = 3;
  int32 portService = 4;
}
message servicesNameRequest {
  string nameService = 1;
}

message ListServiceInstancesResponse {
  bool success = 1;
  repeated ServiceInstance Instances = 2;
}